# codex: use the locally installed Codex and the user's ChatGPT account
# demo: no external calls, deterministic sample result
MARKETING_RUNTIME=codex
CODEX_BIN=codex
# Leave empty to use the account's current Codex default model.
CODEX_MODEL=
PORT=8787
HOST=127.0.0.1
DATA_DIR=.data
